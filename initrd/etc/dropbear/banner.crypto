================Decrypt SHELL=============
user        root
==========================================
  For boot please decrypt your encrypted 
         rootfs on $device with:

cryptsetup -T 5 luksOpen $device cryptroot

==========================================
